import styled from 'styled-components';

const Code = styled.code`
  background: var(--color-background-code);
  color: #addb67;
  border-radius: 4px;
  padding: 4px 8px;
  font-size: 0.9em;
`;

export default Code;
