import styled from 'styled-components';

const Link = styled.a`
  color: var(--color-primary);
  font-weight: inherit;
  font-size: inherit;

  :hover {
    text-decoration: underline;
  }
`;

export default Link;
